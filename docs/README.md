# HotwireBits Docs

A documentation site for the HotwireBits component library, built with Ruby on Rails.

## Setup

```bash
cd docs
bundle install
bin/rails db:create
bin/dev
```

Visit http://localhost:3000
