<%# Textarea component — ERB partial %>
<%# Usage: render 'hotwirebits/textarea', ... %>

<textarea name="<%= name %>" placeholder="<%= placeholder %>" rows="<%= rows %>" <%= 'disabled' if disabled %> <%= 'readonly' if readonly %> <%= 'required' if required %> maxlength="<%= max_length %>" class="<%= textarea_classes %>" <%= tag.attributes(local_assigns) %>><%= value %></textarea>
