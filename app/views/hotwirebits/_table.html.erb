<%# Table component — ERB partial %>
<%# Usage: render 'hotwirebits/table', ... %>

<div class="relative w-full overflow-auto">
  <table class="<%= table_classes %>" <%= tag.attributes(local_assigns) %>>
    <%= content %>
  </table>
</div>
