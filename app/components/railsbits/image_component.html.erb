<figure class="<%= figure_classes %>" <%= tag.attributes(@extra_attrs) %>>
  <img
    src="<%= @src %>"
    alt="<%= @alt %>"
    loading="lazy"
    <% if @width %>width="<%= @width %>"<% end %>
    <% if @height %>height="<%= @height %>"<% end %>
    onerror="<%= @fallback ? "this.src='#{@fallback}'" : 'this.style.display=\\'none\\'' %>"
    class="rounded-lg w-full h-auto object-cover"
    <% if @zoomable %>
      data-controller="rb-image"
      data-action="click->rb-image#zoom"
    data-rb-image-src-value="<%= @src %>"
  <% end %>
  />
  <% if @caption %>
    <figcaption class="mt-2 text-center text-sm text-rb-muted-foreground"><%= @caption %></figcaption>
  <% end %>
</figure>
