<span class="relative inline-flex" <%= tag.attributes(@extra_attrs) %>>
  <span class="<%= dot_classes %>"></span>
<% if @with_pulse %>
  <span class="absolute inline-flex h-full w-full animate-ping rounded-full opacity-75 <%= STATUSES[@status] || 'bg-rb-success' %>"></span>
<% end %>
</span>
