<span class="inline-flex items-center gap-1.5" <%= tag.attributes(@extra_attrs) %>>
  <span class="rounded-full <%= COLORS[@status] || 'bg-gray-400' %> <%= SIZES[@size] || 'h-2.5 w-2.5' %>"></span>
<% if @label %>
  <span class="text-sm text-rb-muted-foreground"><%= @label %></span>
<% end %>
</span>
