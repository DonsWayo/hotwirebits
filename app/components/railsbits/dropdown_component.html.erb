<div class="relative inline-block" data-controller="rb-dropdown" <%= tag.attributes(@extra_attrs) %>>
  <button type="button"
          data-action="click->rb-dropdown#toggle click@window->rb-dropdown#closeOnClickOutside"
    class="inline-flex items-center justify-center rounded-md px-4 py-2 text-sm font-medium text-rb-foreground hover:bg-rb-accent transition-colors cursor-pointer">
    <%= content || @trigger_label %>
    <svg class="ml-2 h-4 w-4 text-rb-muted-foreground" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
  </button>
  <div data-rb-dropdown-target="menu" class="<%= panel_classes %> hidden">
    <% @items.each do |item| %>
      <% if item[:separator] %>
        <div class="-mx-1 my-1 h-px bg-rb-border"></div>
      <% else %>
        <a href="<%= item[:href] %>"
           class="<%= merge_classes(
             'relative flex cursor-pointer select-none items-center gap-2 rounded-sm px-2 py-1.5 text-sm outline-none transition-colors',
             item[:destructive] ? 'text-rb-destructive hover:bg-rb-destructive/10' : 'hover:bg-rb-accent hover:text-rb-accent-foreground'
           ) %>"
           data-action="click->rb-dropdown#close">
          <% if item[:icon] %>
            <span class="h-4 w-4 shrink-0"><%= item[:icon].html_safe %></span>
          <% end %>
          <span><%= item[:label] %></span>
        </a>
      <% end %>
    <% end %>
  </div>
</div>
