<section class="<%= container_classes %>" <%= tag.attributes(@extra_attrs) %>>
  <div class="mx-auto max-w-3xl">
    <% if @title %>
      <h2 class="text-3xl md:text-4xl font-bold text-hw-foreground text-center mb-10"><%= @title %></h2>
    <% end %>
    <div class="divide-y divide-hw-border rounded-lg border border-hw-border" data-controller="hw-accordion">
      <% @items.each_with_index do |item, index| %>
        <div>
          <button type="button"
                  class="flex w-full items-center justify-between px-6 py-4 text-left hover:bg-hw-muted/50 transition-colors"
                  data-action="click->hw-accordion#toggle"
            data-hw-accordion-target="trigger">
            <span class="text-base font-medium text-hw-foreground pr-4"><%= item[:question] %></span>
            <svg class="h-5 w-5 shrink-0 text-hw-muted-foreground transition-transform duration-200" xmlns="http://www.w3.org/2000/svg"
                 viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/>
            </svg>
          </button>
          <div class="hidden px-6 pb-4 text-sm text-hw-muted-foreground leading-relaxed" data-hw-accordion-target="content">
            <%= item[:answer] %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>
