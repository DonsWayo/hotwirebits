<% tag_name = self.class::TAG_MAP[(@as || :p).to_sym] || :p %>
<% style_key = (@as || :p).to_sym %>
<%= tag.public_send(tag_name, content, class: self.class::STYLES[style_key] || self.class::STYLES[:p], **@extra_attrs) %>
